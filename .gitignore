# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local

# npm cache (used during install attempts; includes versioned variants like .npm-cache-p6)
.npm-cache*

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
vite-dev.log
npm-install*.log

# Vite temporary build artifacts
vite.config.ts.timestamp-*.mjs

# Playwright artifacts
test-results/
playwright-report/

# Editor / OS
.DS_Store
*.swp
.idea
.vscode

# WorkBuddy internal state (assistant memory, not project source)
.workbuddy

# Project docs (development docs + large rules PDF ~98MB, intentionally untracked)
docs/
